import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    height: 80,
    width: 250,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 30,
    marginTop: 20,
    borderWidth: 3
  },
  text: {
    fontSize: 20,
    color: 'black'
  },

  refreshContainer: {
    marginTop: 20,
    justifyContent: 'center',
    alignItems: 'center',
  },

  refreshtext: {
    fontFamily: 'BebasNeueRegular-gxj83',
    fontSize: 35,
  },
})
export default styles